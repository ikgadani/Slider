<?xml version="1.0" encoding="UTF-8"?>
<web-app version="6.0" xmlns="https://jakarta.ee/xml/ns/jakartaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd">
    <context-param>
        <param-name>jakarta.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>

    <!-- JSF Servlet Configuration -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>/faces/*</url-pattern>
    </servlet-mapping>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <welcome-file-list>
        <welcome-file>faces/index.xhtml</welcome-file>
    </welcome-file-list>

    <!-- Security Constraint for JSF Slider Pages -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>JSF Slider Pages</web-resource-name>
            <url-pattern>/faces/slider/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>JsfGroup</role-name>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Security Constraint for RESTful Resources -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>REST Resources</web-resource-name>
            <url-pattern>/api/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>RestGroup</role-name>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Security Constraint for Admin Pages -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Admin Pages</web-resource-name>
            <url-pattern>/faces/appuser/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Public Access to Index Page -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Index Page</web-resource-name>
            <url-pattern>/faces/index.xhtml</url-pattern>
        </web-resource-collection>
        <!-- No auth-constraint for public access -->
    </security-constraint>

    <!-- Login Configuration -->
    <login-config>
        <auth-method>FORM</auth-method>
        <realm-name>ApplicationRealm</realm-name>
        <form-login-config>
            <form-login-page>/login.xhtml</form-login-page>
            <form-error-page>/error.xhtml</form-error-page>
        </form-login-config>
    </login-config>

    <!-- Define Roles -->
    <security-role>
        <role-name>Admin</role-name>
    </security-role>
    <security-role>
        <role-name>JsfGroup</role-name>
    </security-role>
    <security-role>
        <role-name>RestGroup</role-name>
    </security-role>
</web-app>
